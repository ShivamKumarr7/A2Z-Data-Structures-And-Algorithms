// Print "GFG" n times using recursion 


#include<bits/stdc++.h>
using namespace std;
void print_recursively(int n)
{
    if(n == 0)return;
    cout<<"GFG"<<" ";
    print_recursively(n-1);
}
int main()
{
    int n;
    cin>>n;
    print_recursively(n);
    return 0;
}
